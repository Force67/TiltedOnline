#pragma once

#include "NetImmerse/NiObject.h"

namespace creation
{
    // Might be in the wrong directory
    class BSTextureSet : public NiObject
    {
    };

    static_assert(sizeof(BSTextureSet) == 0x10);
}
